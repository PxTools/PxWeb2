/* istanbul ignore file */
import { describe, it, expect } from 'vitest';

// Placeholder test file left in place (cannot remove due to tooling) to satisfy Vitest.
// All global setup now lives in setupTests.ts referenced via test.setupFiles.

describe('setupTests placeholder', () => {
  it('passes', () => {
    expect(true).toBe(true);
  });
});
